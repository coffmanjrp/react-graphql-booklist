# React GraphQL Book List

To install the dependencies

```
npm i
```

To run the page move to server directory first

```
cd server
```

To run the server

```
npm run server
```

To run client

```
npm run client
```

To run both

```
npm run dev
```
